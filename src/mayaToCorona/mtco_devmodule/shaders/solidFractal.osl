shader solidFractal(matrix placementMatrix = 0, output color outColor = 0)
{
	matrix m = 1;
	if( isconnected(placementMatrix))
	{
		m = placementMatrix;
	}
	
	point PP = transform(m, P);
	color nc = noise("noise", PP);
	outColor = nc;
}